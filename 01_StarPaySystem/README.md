# StarPaySystem

###  项目简介
这是一个模拟星际交易的控制台支付程序。通过该项目，我深入练习了 Java 的面向对象特性。

###  核心知识点
- **高精度计算**: 使用 `BigDecimal` 避免 0.1+0.2 的精度丢失。
- **设计模式**: 使用 `Singleton` (单例) 确保汇率管理器全局唯一。
- **封装与安全**: 私有化成员变量，限制构造函数访问。
- **内存优化**: 观察包装类 `Integer` 的缓存机制（-128~127）。
- **交互逻辑**: 使用 `Scanner` 与 `while` 循环实现控制台菜单。